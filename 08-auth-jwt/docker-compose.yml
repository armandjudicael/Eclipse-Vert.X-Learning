version: '3.8'
services:
  auth-jwt:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vertx-auth-jwt
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx256m
    restart: unless-stopped
