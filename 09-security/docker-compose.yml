version: '3.8'
services:
  security:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vertx-security
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx256m
    restart: unless-stopped
