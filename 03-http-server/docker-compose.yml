version: '3.8'

services:
  http-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vertx-http-server
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx256m
    restart: unless-stopped
