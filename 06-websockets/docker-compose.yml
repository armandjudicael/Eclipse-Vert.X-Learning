version: '3.8'
services:
  websockets:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vertx-websockets
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx256m
    restart: unless-stopped
